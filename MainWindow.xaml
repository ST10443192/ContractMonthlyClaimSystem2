<Window x:Class="ContractMonthlyClaimSystem2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CMCS Login" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Background="#F0F0F0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Contract Monthly Claiming System" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,20,0,20"
                   Foreground="#2E3440"/>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Width="300">

            <!-- Email -->
            <TextBlock Text="Email:" 
                       Margin="0,0,0,5" 
                       FontWeight="SemiBold"/>
            <TextBox x:Name="txtEmail" 
                     Height="35" 
                     Margin="0,0,0,15"
                     Padding="10,8"
                     BorderBrush="#CCCCCC"
                     BorderThickness="1"/>

            <!-- Password -->
            <TextBlock Text="Password:" 
                       Margin="0,0,0,5" 
                       FontWeight="SemiBold"/>
            <PasswordBox x:Name="txtPassword" 
                         Height="35" 
                         Margin="0,0,0,15"
                         Padding="10,8"
                         BorderBrush="#CCCCCC"
                         BorderThickness="1"/>

            <!-- Role Selection -->
            <TextBlock Text="Role:" 
                       Margin="0,0,0,5" 
                       FontWeight="SemiBold"/>
            <ComboBox x:Name="cmbRole" 
                      Height="35" 
                      Margin="0,0,0,20"
                      Padding="10,8"
                      BorderBrush="#CCCCCC"
                      BorderThickness="1">
                <ComboBoxItem Content="Lecturer"/>
                <ComboBoxItem Content="Academic Manager"/>
            </ComboBox>

            <!-- Login Button -->
            <Button Content="Login" 
                    Height="40" 
                    Background="#4CAF50" 
                    Foreground="White" 
                    BorderThickness="0" 
                    FontSize="16" 
                    FontWeight="SemiBold"
                    Click="Login_Click"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45a049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

        </StackPanel>

        <!-- Footer -->
        <TextBlock Grid.Row="2" 
                   Text="Test Credentials: lecturer@test.com / admin@test.com | Password: 123" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,10"
                   FontSize="10"
                   Foreground="#666666"/>
    </Grid>
</Window>